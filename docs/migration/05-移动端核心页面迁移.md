# 步骤 05：移动端核心页面迁移

## 概述

迁移旧 App 的核心业务页面到新架构，使用 tRPC + TanStack Query 替代手动 fetch + Zustand store。

## 页面迁移清单

### 一期：核心页面
| 旧页面 | 新路径 | 优化点 |
|--------|--------|--------|
| `(tabs)/projects.tsx` | `(app)/(tabs)/projects.tsx` | tRPC 替代 Zustand store |
| `projects/[id].tsx` | `(app)/projects/[id].tsx` | tRPC useQuery |
| `projects/[id]/tasks.tsx` | `(app)/projects/[id]/tasks.tsx` | tRPC |
| `(tabs)/user-profile.tsx` | `(app)/(tabs)/my-work.tsx` | 重命名 + tRPC |
| `(tabs)/profile.tsx` | `(app)/(tabs)/profile.tsx` | Better Auth session |
| task-detail | `(app)/tasks/[id].tsx` | tRPC |
| defect-detail | `(app)/defects/[id].tsx` | tRPC |

### 二期：业务页面
| 旧页面 | 优先级 |
|--------|--------|
| 工时管理 | 中 |
| 考勤打卡 | 中 |
| 工地日志 | 中 |
| 文档管理 | 中 |
| 团队管理 | 中 |

### 三期：辅助页面
| 旧页面 | 优先级 |
|--------|--------|
| 连接/社交 | 低 |
| 通知列表 | 低 |
| 设置 | 低 |
| Onboarding | 低 |

## 迁移模式对比

### 旧模式 (Zustand + 手动 fetch)
```typescript
// stores/project-store.ts
const useProjectStore = create((set) => ({
  projects: [],
  loading: false,
  fetchProjects: async () => {
    set({ loading: true });
    const res = await api.get('/projects');
    set({ projects: res.data, loading: false });
  },
}));

// 页面中
const { projects, loading, fetchProjects } = useProjectStore();
useEffect(() => { fetchProjects(); }, []);
```

### 新模式 (tRPC + TanStack Query)
```typescript
// 页面中 - 直接使用
const { data: projects, isLoading } = trpc.project.list.useQuery({ page: 1, limit: 20 });

// 创建 mutation
const createMutation = trpc.project.create.useMutation({
  onSuccess: () => {
    utils.project.list.invalidate(); // 自动刷新列表
  },
});
```

## 组件迁移策略

### 可直接复用的组件
| 旧组件 | 说明 |
|--------|------|
| `loading-state.tsx` | 骨架加载 UI |
| `confirmation-dialog.tsx` | 确认弹窗 |
| `phone-number-input.tsx` | 手机号输入框 |
| `appbar-menu.tsx` | 顶部菜单 |

### 需要重构的组件
| 旧组件 | 重构原因 |
|--------|----------|
| `enhanced-project-card.tsx` | 去掉 Zustand 依赖，改为 props 驱动 |
| `task-card.tsx` | 同上 |
| `task-form.tsx` | 改用 tRPC mutation |
| `multi-step-project-form.tsx` | 简化步骤，使用 tRPC |

### 不迁移的组件
| 组件 | 原因 |
|------|------|
| `sync-status-bar.tsx` | 取决于离线方案 |
| `voice-project-button.tsx` | AI 功能后期 |
| 实时翻译相关组件 | 后期功能 |

## Claude Code 提示词

```
你是 PulseBuild 项目的移动端开发者。当前项目路径: C:\projects\pulsebuild

前置条件：步骤 04 已完成（认证 + 导航 + Provider 已就绪）。

参考旧项目页面代码:
- 项目列表: C:\projects\pulsebuild-projects\pulsebuild-app\app\(app)\(tabs)\projects.tsx
- 项目详情: C:\projects\pulsebuild-projects\pulsebuild-app\app\(app)\projects\
- 任务相关: C:\projects\pulsebuild-projects\pulsebuild-app\components\tasks\
- 缺陷相关: C:\projects\pulsebuild-projects\pulsebuild-app\components\defects\
- 个人资料: C:\projects\pulsebuild-projects\pulsebuild-app\app\(app)\(tabs)\profile.tsx

参考旧项目组件:
- C:\projects\pulsebuild-projects\pulsebuild-app\components\

## 任务

### Phase 1: 通用组件迁移

1. 创建 `src/components/common/loading-state.tsx`
   - 从旧 app 迁移骨架加载组件
   - 保持 React Native Paper 风格

2. 创建 `src/components/common/empty-state.tsx`
   - 空数据状态显示

3. 创建 `src/components/common/error-state.tsx`
   - 错误状态显示 + 重试按钮

### Phase 2: 项目列表页

1. 重写 `src/app/(app)/(tabs)/projects.tsx`:
   - 使用 `trpc.project.list.useQuery()` 获取项目列表
   - 实现搜索过滤（本地过滤 or 服务端参数）
   - 下拉刷新: refetch()
   - 点击跳转项目详情
   - FAB 按钮创建项目

2. 创建 `src/components/projects/project-card.tsx`:
   - 从旧 app 的 enhanced-project-card.tsx 迁移
   - 移除 Zustand 依赖，改为纯 props 驱动
   - 显示: 项目名、地址、状态、成员数、创建时间

### Phase 3: 项目详情页

1. 创建 `src/app/(app)/projects/[id].tsx`:
   - 使用 `trpc.project.getById.useQuery({ id })` 获取详情
   - Tab 切换: 概览 / 任务 / 缺陷 / 成员
   - 编辑按钮 → 编辑弹窗

2. 创建项目子页面组件:
   - `src/components/projects/project-overview.tsx` - 项目概览
   - `src/components/projects/project-tasks-tab.tsx` - 任务列表 Tab
   - `src/components/projects/project-defects-tab.tsx` - 缺陷列表 Tab
   - `src/components/projects/project-members-tab.tsx` - 成员列表 Tab

### Phase 4: 任务管理

1. 创建 `src/components/tasks/task-card.tsx`:
   - 迁移旧 app task-card，纯 props 驱动
   - 显示: 任务名、状态、负责人、截止日

2. 创建 `src/components/tasks/task-form.tsx`:
   - 创建/编辑任务表单
   - 使用 tRPC mutation

3. 创建 `src/app/(app)/tasks/[id].tsx`:
   - 任务详情页
   - 子任务列表
   - 状态变更操作

### Phase 5: 缺陷管理

1. 创建 `src/components/defects/defect-card.tsx`
2. 创建 `src/components/defects/defect-form.tsx`
3. 创建 `src/app/(app)/defects/[id].tsx`

### Phase 6: 我的工作页

1. 重写 `src/app/(app)/(tabs)/my-work.tsx`:
   - 使用 `trpc.task.listMyAssigned.useQuery()` 获取我的任务
   - 分组显示: 今日到期 / 进行中 / 即将到期
   - 快速状态切换

### Phase 7: 个人中心

1. 重写 `src/app/(app)/(tabs)/profile.tsx`:
   - 使用 authClient.useSession() 获取当前用户
   - 显示: 头像、姓名、手机号、公司信息
   - 编辑资料: tRPC mutation
   - 退出登录: authClient.signOut()

## 规范要求
- 所有服务端数据通过 tRPC hooks 获取，不使用 Zustand
- 组件使用 React Native Paper 组件库
- 列表页使用 FlatList + 分页
- 表单使用受控组件
- 页面保持单一职责，复杂逻辑提取到 hooks
- 迁移组件 UI 时保持旧 app 的视觉风格
- 类型安全：所有 tRPC 返回数据自动推导类型
```

---

## 需要你确认的事项

1. **项目详情的 Tab 设计**: 概览/任务/缺陷/成员 这 4 个 Tab 是否合适？
2. **搜索**: 项目列表搜索是本地过滤还是服务端搜索？
3. **组件复用粒度**: 旧 app 有 42+ 组件目录，哪些需要保持？哪些可以合并/删除？
4. **表单库**: 是否引入 react-hook-form？旧 app 用受控组件。
5. **列表分页**: 无限滚动 vs 分页按钮？
